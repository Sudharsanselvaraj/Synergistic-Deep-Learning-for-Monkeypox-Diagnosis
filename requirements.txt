# Tri-Net v2 runtime dependencies — mirrors [project.dependencies] in pyproject.toml.
# Preferred install is `pip install -e .` (or `pip install mpox-trinet`); this file exists for
# environments that pin from a flat list. Keep it in sync with pyproject.toml.

# Core DL
tensorflow==2.16.2

# Numerics / data
numpy==1.26.4
pandas==2.2.2
scikit-learn==1.5.1
scipy==1.13.1

# Metaheuristic optimization (PSO ensemble weights)
pyswarms==1.3.0

# Classical baselines for the symptom module
xgboost==2.1.1

# Images
pillow==10.4.0
opencv-python==4.10.0.84

# Viz
matplotlib==3.9.2
seaborn==0.13.2

# Utilities
joblib==1.4.2
tqdm==4.66.5
kaggle==2.2.3

# CLI + config
typer>=0.12
pyyaml>=6.0

# Apple Silicon (M-series) GPU acceleration — macOS arm64 ONLY.
# tensorflow-metal is not installable on Linux/Windows, so it is left out of the hard list.
# On Apple Silicon, install it explicitly or use the extra:  pip install "mpox-trinet[metal]"
# tensorflow-metal==1.1.0
